<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <modules>
        <module>hotel-service</module>
        <module>booking-service</module>
        <module>payment-service</module>
        <module>customer-service</module>
        <module>commons</module>
    </modules>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>com.hotel.booking.system</groupId>
    <artifactId>hotel-booking-system</artifactId>
    <version>${revision}</version>

    <packaging>pom</packaging>

    <properties>
        <revision>1.0.0</revision>
        <!-- O parent do Spring Boot deriva `maven.compiler.release` desta property,
             então ela sozinha governa source, target e release dos cinco módulos. -->
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.hotel.booking.system</groupId>
                <artifactId>commons</artifactId>
                <version>${revision}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- O `@ServiceConnection`, que aponta o contexto de teste para o container.
             É agnóstico de banco: o módulo do Testcontainers correspondente ao banco é
             declarado por serviço, ao lado do driver de produção, para que os dois
             troquem juntos na hora da migração. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-docker-compose</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <!-- O `annotationProcessorPaths` é obrigatório a partir do JDK 23:
                         o javac deixou de descobrir processadores pelo classpath e passou
                         a assumir `-proc:none` quando ninguém declara o caminho. Sem este
                         bloco o Lombok não roda, e a falha não se anuncia — ela aparece
                         mais adiante como `cannot infer type arguments` no código que
                         dependia do construtor ou do getter que não foram gerados.

                         As versões vêm do `dependencyManagement` do BOM do Spring Boot,
                         resolução que o maven-compiler-plugin suporta desde a 3.12.0. -->
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                        <path>
                            <groupId>org.springframework.boot</groupId>
                            <artifactId>spring-boot-configuration-processor</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <!-- Os testes que sobem um container ficam sob o failsafe, e não sob o
                 surefire, por causa do custo: `mvn test` continua sendo a volta rápida
                 de quem está mexendo em domínio, e `mvn verify` é a volta que exige
                 Docker. A separação é pelo sufixo `IT`, que o surefire não coleta e o
                 failsafe coleta por padrão — nenhuma configuração de includes.

                 O goal `verify` é obrigatório: sem ele o failsafe registra a falha no
                 relatório e o build passa mesmo assim. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
